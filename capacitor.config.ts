import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.preachingpractice',
  appName: 'Preaching practice',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
